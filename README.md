# Petstore Monorepo

This repository contains a minimal scaffold for a pet e-commerce application.

Structure:
- backend: Spring Boot 3 (Java 17) + Spring Data JPA + PostgreSQL
- frontend: Vite + React + TypeScript + Tailwind CSS + MUI
- infra: docker-compose for local PostgreSQL

See subfolders for run instructions.